# CHANGELOG — superpowers-full

## v0.1 — 2026-05-09 (Sprint 5.2 v3)

Stub funcional. Diferenciada explícitamente de:

- `quality-mindset` (core, universal).
- `superpowers-pr` (catálogo, PR-específica).

Para evitar solapamiento, esta skill cubre el set completo (~80 skills) del repo upstream cuando el proyecto justifica complejidad alta (TDD estricto, spec-driven, debug riguroso, refactor seguro).

Pendiente: inventariar sub-skills, decidir cuáles entran como skills independientes al `_catalog/`.

Origen: video YouTube anotado por Aldo (Sprint 5.2). Aldo confirmó que "superpowers" sin diferenciar era redundante con `superpowers-pr` + `quality-mindset`. Resolución: incluir como skill diferenciada (`superpowers-full`) con scope claro.
