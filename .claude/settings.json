{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "_comment": "Settings de proyecto del starter Mazelab. Hooks PreToolUse/PostToolUse sobre Agent alimentan el dashboard en vivo (dashboard/state.json) sin que Aldot tenga que hacer nada manual. Si interfieren, renombra este archivo a settings.json.disabled y reinicia Claude Code.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Agent",
        "hooks": [
          {
            "type": "command",
            "command": "node \"$CLAUDE_PROJECT_DIR/scripts/dashboard_hook.js\" pre",
            "timeout": 5
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "Agent",
        "hooks": [
          {
            "type": "command",
            "command": "node \"$CLAUDE_PROJECT_DIR/scripts/dashboard_hook.js\" post",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
